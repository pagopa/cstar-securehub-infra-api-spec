name: Validate all OpenAPI YAML with Spectral (Soft Fail - Fixed)

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate-openapi:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Spectral CLI
        run: npm install -g @stoplight/spectral-cli

      - name: Lint OpenAPI .yml files (Soft Fail - Fixed)
        run: |
          echo "🔍 Searching for OpenAPI YAML files..."
          summary=""
          failed_files=()

          find src/idpay/apim/api -type f -name "*.yml" | while read file; do
            if grep -qE "^openapi:|^swagger:" "$file"; then
              echo "🔎 Validating OpenAPI file: $file"
              # Usa spectral senza specificare -r (usa il ruleset integrato per OpenAPI)
              if spectral lint "$file"; then
                echo "✅ $file: PASSED"
                summary="$summary\n✅ $file: PASSED"
              else
                echo "❌ $file: FAILED"
                summary="$summary\n❌ $file: FAILED"
                failed_files+=("$file")
              fi
            else
              echo "⚠️ Skipping non-OpenAPI YAML: $file"
            fi
          done

          echo -e "\n📋 Validation Summary:"
          echo -e "$summary"

          if [ ${#failed_files[@]} -ne 0 ]; then
            echo "⚠️ Completed with errors in ${#failed_files[@]} OpenAPI file(s), but not failing the job (soft fail)."
          else
            echo "✅ All OpenAPI files passed validation."
          fi
